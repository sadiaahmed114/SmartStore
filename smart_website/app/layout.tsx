import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "./components/Navbar";
import CartProvider from "./components/Providers";
import ShoppingCartModal from "./components/ShoppingCartModel";




const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  // const router = useRouter();
  // const isCheckoutPage = router.pathname === "/checkout"; 
  return (
    <html lang="en">
      <body className={inter.className}>
      {/* {!isCheckoutPage && <Navbar />}     */}
       <CartProvider>
       <Navbar />         
       <ShoppingCartModal/>
        {children}
       </CartProvider>
        </body>
    </html>
  );
}

// import type { Metadata } from "next";
// import { Inter } from "next/font/google";
// import "./globals.css";
// import Navbar from "./components/Navbar";
// import CartProvider from "./components/Providers";
// import ShoppingCartModal from "./components/ShoppingCartModel";

// const inter = Inter({ subsets: ["latin"] });

// export const metadata: Metadata = {
//   title: "Create Next App",
//   description: "Generated by create next app",
// };

// interface RootLayoutProps {
//   renderNavbar?: boolean; // Prop to control whether to render the Navbar
// }

// export default function RootLayout({
//   children,
//   renderNavbar = true, // Default value is true to render Navbar by default
// }: RootLayoutProps & Readonly<{ children: React.ReactNode }>) {
//   return (
//     <html lang="en">
//       <body className={inter.className}>
//         {renderNavbar && <Navbar />} {/* Render Navbar based on prop */}
//         <CartProvider>
//           <ShoppingCartModal />
//           {children}
//         </CartProvider>
//       </body>
//     </html>
//   );
// }